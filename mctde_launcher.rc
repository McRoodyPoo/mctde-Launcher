#include "resource.h"

// The first icon resource (lowest ID) becomes the exe's Explorer/taskbar icon.
IDI_APPICON ICON "mctde.ico"

// Trimmed white-on-black Artorias silhouette, drawn themed (inverted in light mode).
IDB_ARTORIAS BITMAP "artorias.bmp"

// Full pre-composed banners used as the whole-window background, replacing the programmatic
// header in that theme. Both are the "Dark Souls: Prepare To Die Edition" logo + Artorias figure
// over a black|cream split (mirrored between themes) with an "mctde" subtitle, zoomed in and
// anchored to the top-right, fit 1:1 to the 524x346 client so they fill without stretching.
IDB_BG_DARK  BITMAP "mctde_bg_dark.bmp"
IDB_BG_LIGHT BITMAP "mctde_bg_light.bmp"

// Changelogs embedded as raw text so the launcher stays self-contained.
IDR_CHANGELOG_LAUNCHER  RCDATA "CHANGELOG.md"
IDR_CHANGELOG_MCTDELINK RCDATA "mctde-Link.CHANGELOG.md"
